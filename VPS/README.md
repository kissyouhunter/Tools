DD VPS linux

```
#UBUNTU 20.04
bash <(wget --no-check-certificate -qO- 'https://raw.githubusercontent.com/MoeClub/Note/master/InstallNET.sh') -u 20.04 -v 64 -p "password" -port "22" --mirror "http://mirrors.ustc.edu.cn/ubuntu/"
```
